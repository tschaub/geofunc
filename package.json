{
  "name": "geofunc",
  "version": "0.2.0",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint lib test",
    "pretest": "npm run lint",
    "test": "jest",
    "start": "jest --watch"
  },
  "devDependencies": {
    "eslint": "^4.9.0",
    "eslint-config-tschaub": "^9.0.0",
    "jest": "^21.2.1"
  },
  "eslintConfig": {
    "extends": "tschaub"
  }
}
