{
  "name": "geofunc",
  "version": "0.0.0",
  "main": "lib/index.js",
  "scripts": {
    "pretest": "eslint lib test",
    "test": "jest",
    "start": "jest --watch"
  },
  "devDependencies": {
    "eslint": "^3.10.1",
    "eslint-config-tschaub": "^6.0.0",
    "jest": "^17.0.2"
  },
  "eslintConfig": {
    "extends": "tschaub"
  }
}
